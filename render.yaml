services:
  - type: web
    name: employee-management-api
    runtime: node
    plan: free
    rootDir: Backend
    buildCommand: npm install && npm run build && npm run db:fix-refresh-token
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        sync: false
      - key: JWT_SECRET
        sync: false
      - key: JWT_REFRESH_SECRET
        sync: false
      - key: CLIENT_URL
        value: https://employee-management-livid-rho.vercel.app
